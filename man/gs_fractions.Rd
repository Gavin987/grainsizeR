% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fractions.R
\name{gs_fractions}
\alias{gs_fractions}
\title{Calculate grain-size fraction percentages}
\usage{
gs_fractions(
  x,
  scheme = "wentworth_major",
  normalize = c("none", "fine_earth"),
  interpolation_scale = "phi",
  unresolved = c("warn_na", "error"),
  extrapolate = "error"
)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{scheme}{Built-in fraction scheme name.}

\item{normalize}{Normalization mode. \code{"none"} returns whole-sample
percentages. \code{"fine_earth"} requires a scheme with a \code{gravel} component,
excludes gravel rows, and normalizes the remaining non-gravel fractions
against the non-gravel total.}

\item{interpolation_scale}{Interpolation scale passed to \code{gs_percent_finer()}.}

\item{unresolved}{Behavior when required thresholds cannot be calculated.
\code{"warn_na"} warns and returns \code{NA} for affected components. \code{"error"}
throws an error.}

\item{extrapolate}{Extrapolation behavior passed to \code{gs_percent_finer()}.
The default \code{"error"} avoids silent extrapolation into open-ended terminal
classes.}
}
\value{
A tibble with one row per sample and scheme component.
}
\description{
\code{gs_fractions()} calculates sediment or soil fraction percentages using a
named built-in particle-size scheme. Schemes are treated as complete,
non-overlapping particle-size partitions. Fractions are calculated from
cumulative percent-finer values at scheme thresholds by calling
\code{gs_percent_finer()} for thresholds inside the observed finite size range.
}
\details{
Scheme thresholds do not need to match observed grain-size boundaries. When
thresholds such as 0.002, 0.020, 0.050, 0.060, or 0.063 mm are bracketed by finite class
boundaries, percent-finer values are interpolated on the cumulative curve.
Fraction and texture functions automatically use the normalized particle-size
scale from \code{gsd_tbl}; users do not need to specify millimetres or
micrometres after import.
Thresholds above the largest observed finite boundary resolve to 100 percent
finer, and thresholds below the smallest observed finite boundary resolve to
0 percent finer. This returns absent particle-size classes as zero rather
than \code{NA}, so complete schemes close to 100 percent for samples whose
retained percentages sum to 100. \code{NA} is reserved for thresholds that are
genuinely unresolved inside the finite observed size range. Fraction schemes
do not extrapolate unless \code{extrapolate = "warn_linear"} is passed explicitly.

Fraction thresholds interpolate using \code{gs_percent_finer()}'s size-as-\code{x}
direction, so the tied-cumulative-value scenario that \code{gs_d_values()}
resolves deterministically (see its documentation) cannot occur here.
}
