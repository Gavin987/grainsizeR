% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/d-spread.R
\name{gs_d_spread}
\alias{gs_d_spread}
\title{Calculate GRADISTAT-style D-spread descriptors}
\usage{
gs_d_spread(
  x,
  scale = c("um", "mm"),
  interpolation_scale = c("phi", "log_um", "linear_um"),
  extrapolate = c("error", "warn_linear")
)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{scale}{Metric reporting scale for D-values and differences. Supported
values are \code{"um"} and \code{"mm"}.}

\item{interpolation_scale}{Interpolation scale passed to \code{gs_d_values()}.}

\item{extrapolate}{Extrapolation behavior passed to \code{gs_d_values()}.}
}
\value{
A tibble with one row per sample and D-spread descriptor columns.
}
\description{
\code{gs_d_spread()} calculates D-value spread descriptors commonly reported in
GRADISTAT-style grain-size summaries. It reuses \code{gs_d_values()} for D10,
D25, D50, D75, and D90, then derives D90/D10, D90 - D10, D75/D25, and
D75 - D25.
}
\details{
Ratios and differences are metric descriptors. \code{scale = "um"} reports
D-values and differences in micrometers, while \code{scale = "mm"} reports them
in millimeters. \code{scale = "phi"} is not supported because phi differences are
not the same parameter as metric D-value spread differences. Optional log
ratio columns are calculated from positive metric D-values.

Open-tail behavior follows \code{gs_d_values()}: by default unresolved requested
percentiles throw an error, and \code{extrapolate = "warn_linear"} explicitly
allows linear extrapolation and marks affected samples with
\code{any_extrapolated = TRUE}.
}
\examples{
gsd <- as_gsd_tbl(
  data.frame(
    sample = rep("A", 5),
    size_mm = c(2, 1, 0.5, 0.25, 0.125),
    retained = c(5, 15, 35, 30, 15)
  ),
  sample,
  size_mm,
  retained,
  value_type = "percent"
)

gs_d_spread(gsd, extrapolate = "warn_linear")
}
