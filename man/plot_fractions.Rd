% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fractions.R
\name{plot_fractions}
\alias{plot_fractions}
\title{Plot grain-size fraction composition}
\usage{
plot_fractions(
  x,
  scheme = "wentworth_major",
  normalize = "none",
  sample = NULL,
  sample_id = NULL,
  fill_palette = c("default", "YlOrBr", "none"),
  na_to_zero = FALSE
)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{scheme}{Built-in fraction scheme name passed to \code{gs_fractions()}.}

\item{normalize}{Normalization mode passed to \code{gs_fractions()}.}

\item{sample}{Optional sample selector. A character value selects by sample
ID; a numeric value selects by one-based sample index using the order in
which samples appear in \code{x}.}

\item{sample_id}{Optional character vector of sample identifiers to include.
Kept for backward compatibility; use \code{sample} for new code.}

\item{fill_palette}{Fill palette. \code{"default"} uses ggplot2 defaults,
\code{"YlOrBr"} uses \code{grDevices::hcl.colors()} with a yellow-orange-brown
sequence, and \code{"none"} leaves the scale unchanged.}

\item{na_to_zero}{Should unresolved fraction percentages be plotted as zero?
The default \code{FALSE} preserves \code{NA} values returned by \code{gs_fractions()}.
Use \code{TRUE} to draw stacked bars without dropping components whose
thresholds could not be resolved from the available grain-size classes.
This affects only the plotted data and does not change the underlying
\code{gs_fractions()} calculation.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_fractions()} plots fraction percentages from \code{gs_fractions()} as
stacked bars with one bar per sample. Fraction components use the normalized
particle-size scale from \code{gsd_tbl}; users do not need to specify size units
for plotting after import.
}
\examples{
x <- data.frame(
  sample_id = c("A", "A", "A", "B", "B", "B"),
  size_mm = rep(c(2, 0.5, 0.063), 2),
  retained_proportion = c(0.20, 0.50, 0.30, 0.10, 0.60, 0.30)
)
gsd <- as_gsd_tbl(x, sample_id, size_mm, retained_proportion)
plot_fractions(gsd, scheme = "wentworth_major")
plot_fractions(gsd, sample = 1, scheme = "wentworth_major")
plot_fractions(gsd, scheme = "gravel_sand_mud", fill_palette = "YlOrBr")
}
