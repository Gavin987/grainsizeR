% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modes.R
\name{gs_modes}
\alias{gs_modes}
\title{Identify modal grain-size classes}
\usage{
gs_modes(x, n_modes = 3)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{n_modes}{Number of modal classes to return per sample.}
}
\value{
A tibble with exactly \code{n_modes} rows per sample and modal class
descriptors. Samples with fewer observed modal classes are padded with
\code{NA} descriptor fields and \code{mode_status = "not_observed"}.
}
\description{
\code{gs_modes()} reports the largest retained grain-size classes in each sample.
The output is intended for GRADISTAT-style summaries that list primary,
secondary, and tertiary modes. Modes are ranked by retained class percentage,
with deterministic tie ordering by the original coarse-to-fine class order.
}
\details{
The reported mode is a modal class midpoint, not a true continuous density
mode. Open-ended terminal classes do not have reliable midpoints; if an
open-ended class is selected, midpoint fields are \code{NA} and \code{mode_status} is
\code{"open_interval"}. Tied retained percentages are marked with
\code{mode_status = "tied"}. The \code{sample_modality} column is an operational
descriptor based on retained class frequencies, not a formal mixture model.
}
\examples{
gsd <- as_gsd_tbl(
  data.frame(
    sample = rep("A", 5),
    size_mm = c(2, 1, 0.5, 0.25, 0.125),
    retained = c(5, 35, 15, 30, 15)
  ),
  sample,
  size_mm,
  retained,
  value_type = "percent"
)

gs_modes(gsd)
}
