% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{gs_diagnostics}
\alias{gs_diagnostics}
\title{Diagnose grain-size data quality and resolvability}
\usage{
gs_diagnostics(
  x,
  d_values = c(5, 10, 16, 25, 50, 75, 84, 90, 95),
  thresholds_um = c(2, 20, 50, 60, 62.5, 63, 2000),
  fraction_schemes = c("wentworth_major", "gradistat", "usda", "isss", "uk_ssew"),
  retained_tolerance = 1e-06,
  fine_boundary_um = 63,
  hydrometer_trigger_percent = 10,
  extrapolate = c("error", "warn_linear"),
  output = c("long", "wide", "summary")
)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{d_values}{Numeric D-value percentiles to check.}

\item{thresholds_um}{Numeric grain-size thresholds, in micrometers, to check
with the package percent-finer convention.}

\item{fraction_schemes}{Built-in fraction schemes to check for threshold
resolvability.}

\item{retained_tolerance}{Tolerance for retained percentages summing to 100.}

\item{fine_boundary_um}{Boundary used for fine-resolution and hydrometer
workflow diagnostics.}

\item{hydrometer_trigger_percent}{Workflow trigger for the percent finer
than \code{fine_boundary_um}.}

\item{extrapolate}{Extrapolation behavior passed to lower-level
resolvability checks. The default \code{"error"} reports open-tail limitations
instead of extrapolating.}

\item{output}{Output shape. \code{"long"} returns one row per sample and check.
\code{"summary"} returns counts by sample. \code{"wide"} returns compact status
columns by sample.}
}
\value{
A tibble of diagnostics.
}
\description{
\code{gs_diagnostics()} reports data-quality and computational-resolvability
checks for a \code{gsd_tbl}. It is designed to be run before D-values,
percent-finer thresholds, fraction schemes, summary tables, or texture
workflows.
}
\details{
Diagnostics are not a replacement for domain judgment. The hydrometer
trigger check is a workflow diagnostic, not a universal scientific rule.
Open-ended terminal bins are reported explicitly and are not silently treated
as bounded intervals.
}
