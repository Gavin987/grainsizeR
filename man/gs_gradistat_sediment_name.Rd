% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradistat-sediment-name.R
\name{gs_gradistat_sediment_name}
\alias{gs_gradistat_sediment_name}
\title{Compose GRADISTAT-style sediment names}
\usage{
gs_gradistat_sediment_name(
  x,
  basis = c("auto", "gravel_sand_mud", "sand_silt_clay_no_gravel")
)
}
\arguments{
\item{x}{A data frame. It may be output from \code{classify_texture()} for
\code{scheme = "gradistat"}, or it may contain \code{gravel}, \code{sand}, and \code{mud}
columns, \code{sand}, \code{silt}, and \code{clay} columns, or both.}

\item{basis}{Preferred classification basis. \code{"auto"} uses an existing
\code{ternary_basis} column when present, otherwise it prefers
\code{"gravel_sand_mud"} when \code{gravel}, \code{sand}, and \code{mud} are available and
\code{"sand_silt_clay_no_gravel"} when only \code{sand}, \code{silt}, and \code{clay} are
available.}
}
\value{
A tibble containing the input rows with \code{textural_group_class_id},
\code{textural_group}, \code{mini_texture_class_id}, \code{mini_texture_class},
\code{dominant_gravel_class}, \code{dominant_sand_class}, \code{dominant_silt_class},
\code{sediment_name}, \code{sediment_name_status}, \code{sediment_name_method}, and
\code{notes} appended or updated.
}
\description{
\code{gs_gradistat_sediment_name()} appends GRADISTAT-style sediment-name fields
to a data frame. It is intended for data already classified with
\code{classify_texture(..., scheme = "gradistat", method = "rules")}, or for data
frames containing the GRADISTAT major components needed to compute the
textural group. \verb{TEXTURAL GROUP} and \verb{SEDIMENT NAME} are distinct outputs:
the textural group is the gravel-sand-mud or no-gravel sand-silt-clay class,
while the sediment name may add dominant size-subclass wording when those
subclass percentages are supplied.
}
\details{
The function re-expresses decision-table behavior recorded from the
user-provided GRADISTAT v8 workbook and Blott and Pye (2001). It does not
copy VBA source code. GRADISTAT ternary plotting is handled separately and
is not implemented by this function.
}
\examples{
classified <- classify_texture(
  data.frame(
    sample_id = "A",
    gravel = 0,
    sand = 95,
    mud = 5,
    fine_sand = 70,
    medium_sand = 25
  ),
  scheme = "gradistat",
  method = "rules",
  basis = "gravel_sand_mud"
)

gs_gradistat_sediment_name(classified)
}
