% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-wide.R
\name{read_gsd_wide}
\alias{read_gsd_wide}
\title{Read wide-format grain-size data from a CSV file}
\usage{
read_gsd_wide(
  file,
  size_col = 1,
  size_unit = "auto",
  value_type = "percent",
  measurement_method = NA_character_
)
}
\arguments{
\item{file}{Path to a CSV file.}

\item{size_col}{Column containing grain-size class labels or thresholds.
This can be a one-based column index or a column name.}

\item{size_unit}{Unit for \code{size_col}. Supported values are \code{"auto"},
\code{"mm"}, \code{"um"}, and \code{"phi"}. \code{"auto"} treats finite positive values
greater than or equal to 1000 as micrometres and otherwise treats values
as millimetres. Explicit \code{"mm"} and \code{"um"} values override detection.}

\item{value_type}{Scale for sample values. Supported values are
\code{"proportion"}, \code{"percent"}, and \code{"weight"}.}

\item{measurement_method}{Measurement method to store in the output. A
single string is recycled to all rows.}
}
\value{
A \code{gsd_tbl} tibble with canonical columns including \code{sample_id},
\code{bin_id}, \code{raw_size_um}, \code{size_lower_um}, \code{size_upper_um},
\code{retained_percent}, \code{cum_finer_percent}, \code{cum_coarser_percent},
\code{is_open_lower}, \code{is_open_upper}, and \code{measurement_method}.
}
\description{
\code{read_gsd_wide()} reads a table where grain-size classes are stored in rows
and sample identifiers are stored in columns. Values are retained
proportions, retained percentages, or weights.
}
\details{
Numeric size labels such as \code{"2"} and \code{"0.0625"} are interpreted as class
thresholds. Terminal fine labels such as \code{"<0.0625"} in a strict
Wentworth-style example are parsed as the numeric threshold while still
producing an open-ended fine class in the returned \code{gsd_tbl}. A size label
of \code{"0"} is treated as a pan or lower open-ended row and imported with the
package's 1 um lower-tail marker.
}
