% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-trigon.R
\name{plot_texture_triangle}
\alias{plot_texture_triangle}
\title{Plot samples on a texture ternary plot}
\usage{
plot_texture_triangle(
  x,
  scheme = c("gradistat", "usda_tt", "isss", "uk_ssew"),
  components = NULL,
  normalize = "none",
  sample_id = NULL,
  labels = TRUE,
  polygons = NULL,
  show_polygons = TRUE,
  show_polygon_labels = TRUE,
  polygon_alpha = 0.15,
  classify = FALSE,
  basis = c("gravel_sand_mud", "sand_silt_clay_no_gravel"),
  point_id = NULL,
  show_boundaries = TRUE,
  show_classes = TRUE,
  show_class_labels = show_classes,
  show_sample_labels = labels,
  sample_label_size = 3,
  class_label_size = 2.5,
  label_style = c("inside", "callout", "none")
)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{scheme}{Fraction or user polygon scheme.}

\item{components}{Optional character vector of three component names in
left, right, top order.}

\item{normalize}{Normalization mode passed to \code{gs_fractions_wide()}.}

\item{sample_id}{Optional character vector of sample identifiers to include.}

\item{labels}{Should sample labels be drawn?}

\item{polygons}{Optional user-supplied texture polygon data.}

\item{show_polygons}{Should supplied polygons be drawn?}

\item{show_polygon_labels}{Should polygon class labels be drawn?}

\item{polygon_alpha}{Fill alpha for polygon overlays.}

\item{classify}{Should sample points be classified with \code{classify_texture()}?}

\item{basis}{GRADISTAT ternary plotting basis. Supported values are
\code{"gravel_sand_mud"} and \code{"sand_silt_clay_no_gravel"}.}

\item{point_id}{Optional column name used for point labels in GRADISTAT
data-frame plots.}

\item{show_boundaries}{Should GRADISTAT classification boundaries be drawn?}

\item{show_classes}{Should GRADISTAT class labels be drawn?}

\item{show_class_labels}{Alias for \code{show_classes}.}

\item{show_sample_labels}{Should sample labels be drawn?}

\item{sample_label_size}{Text size for sample labels.}

\item{class_label_size}{Text size for class labels.}

\item{label_style}{Label style for GRADISTAT class labels. \code{"inside"} and
\code{"callout"} use the current readable label placement, and \code{"none"}
suppresses them.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_texture_triangle()} is retained as a stable compatibility function
name, but it creates texture ternary plots. Prefer
\code{plot_texture_ternary()} in new code and prose. Both functions plot
summarized ternary component percentages and optional user-supplied texture
polygons. A fraction scheme is the rule used by \code{gs_fractions()} to convert
size-bin data into components, a ternary basis is the three-component set
drawn on the diagram, and a texture system is the classification or diagram
style selected by \code{scheme}.
The package draws the ternary diagram with ggplot2 and does not depend on
external ternary plotting packages.
}
\details{
The intended GRADISTAT workflow is to read grain-size data, compute
fractions with \code{gs_fractions()} or \code{gs_fractions_wide()}, then plot those
summarized components. For \code{scheme = "gradistat"}, use
\code{basis = "gravel_sand_mud"} with \code{gravel}, \code{sand}, and \code{mud} components, or
\code{basis = "sand_silt_clay_no_gravel"} with \code{sand}, \code{silt}, and \code{clay}
components. Official \code{gs_fractions()} long output, official
\code{gs_fractions_wide()} output with \verb{*_percent} columns, and canonical
summarized tables with component columns are supported. Component column
matching is case-insensitive, so \code{Sand} and \code{SAND} are treated as \code{sand};
arbitrary spelling, punctuation, or suffix variants are not interpreted.
Raw \code{gsd_tbl} input is not plotted directly for GRADISTAT ternary diagrams.

For \code{scheme = "usda_tt"} and data-frame inputs, the function accepts
summarized \code{sand}, \code{silt}, and \code{clay} percentage columns and draws USDA
major-class boundaries. The existing gsd_tbl and user-supplied polygon
workflows for non-GRADISTAT texture plotting are preserved.
}
\examples{
gsm <- data.frame(
  sample_id = c("A", "B", "C"),
  gravel = c(0, 10, 40),
  sand = c(95, 80, 40),
  mud = c(5, 10, 20)
)

plot_texture_ternary(
  gsm,
  scheme = "gradistat",
  basis = "gravel_sand_mud",
  point_id = "sample_id"
)

ssc <- data.frame(
  sample_id = c("A", "B", "C"),
  sand = c(95, 60, 20),
  silt = c(3, 30, 60),
  clay = c(2, 10, 20)
)

plot_texture_ternary(
  ssc,
  scheme = "gradistat",
  basis = "sand_silt_clay_no_gravel",
  point_id = "sample_id"
)
}
