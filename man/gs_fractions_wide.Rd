% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fractions.R
\name{gs_fractions_wide}
\alias{gs_fractions_wide}
\title{Calculate grain-size fraction percentages in wide form}
\usage{
gs_fractions_wide(
  x,
  scheme = "wentworth_major",
  normalize = c("none", "fine_earth"),
  interpolation_scale = "phi",
  unresolved = c("warn_na", "error"),
  extrapolate = "error"
)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{scheme}{Built-in fraction scheme name.}

\item{normalize}{Normalization mode. \code{"none"} returns whole-sample
percentages. \code{"fine_earth"} requires a scheme with a \code{gravel} component,
excludes gravel rows, and normalizes the remaining non-gravel fractions
against the non-gravel total.}

\item{interpolation_scale}{Interpolation scale passed to \code{gs_percent_finer()}.}

\item{unresolved}{Behavior when required thresholds cannot be calculated.
\code{"warn_na"} warns and returns \code{NA} for affected components. \code{"error"}
throws an error.}

\item{extrapolate}{Extrapolation behavior passed to \code{gs_percent_finer()}.
The default \code{"error"} avoids silent extrapolation into open-ended terminal
classes.}
}
\value{
A tibble with one row per sample and component percentage columns.
}
\description{
\code{gs_fractions_wide()} is a convenience wrapper around \code{gs_fractions()} that
returns one row per sample with one percentage column per fraction component.
See \code{gs_fractions()}'s Details for the nominal sieve-mesh equivalence table
and the pan-mass-aware below-boundary resolution logic this wrapper
inherits unchanged.
}
