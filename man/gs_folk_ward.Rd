% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/folkward.R
\name{gs_folk_ward}
\alias{gs_folk_ward}
\title{Calculate Folk and Ward graphical grain-size statistics}
\usage{
gs_folk_ward(
  x,
  interpolation_scale = "phi",
  extrapolate = c("error", "warn_linear"),
  include_descriptions = TRUE
)
}
\arguments{
\item{x}{A valid \code{gsd_tbl} object.}

\item{interpolation_scale}{Interpolation scale passed to \code{gs_d_values()}.}

\item{extrapolate}{Extrapolation behavior passed to \code{gs_d_values()}.}

\item{include_descriptions}{Should descriptive Folk and Ward class labels be
included?}
}
\value{
A tibble with one row per sample and Folk and Ward graphical
statistics.
}
\description{
\code{gs_folk_ward()} calculates Folk and Ward graphical statistics from
boundary-interpolated grain-size percentiles. Percentiles follow the package
convention where \code{D_p} is the grain size at which \code{p} percent of the sample
is finer.
}
\details{
The underlying \code{D_p} percentiles are computed by \code{gs_d_values()},
including its deterministic tie-breaking rule for percentiles that fall
on a plateau caused by consecutive zero-retained classes (see
\code{gs_d_values()} for details).
}
